# Optional: copy to .env for local overrides (none required for the public portfolio).
# Restart the dev server after changes: npm run dev
#
# Docker Compose reads this file for `docker compose up --build`.
# Optional: PORT=8080 (or another port) for host→container mapping.
